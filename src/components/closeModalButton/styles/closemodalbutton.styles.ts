import styled from "styled-components";

export const SCloseModalButton = styled.div`
	position: absolute;
	display: flex;
	justify-content: center;
	align-items: center;
	top: 0;
	right: 0;
	width: 40px;
	height: 40px;
	cursor: pointer;
`